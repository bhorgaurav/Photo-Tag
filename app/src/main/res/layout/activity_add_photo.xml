<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_photo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.csulb.android.assignment2.AddPhotoActivity">

    <ImageButton
        android:id="@+id/imagebutton_take_photo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:minHeight="120dp"
        android:minWidth="120dp"
        android:src="@drawable/ic_camera" />

    <TextView
        android:id="@+id/textview_image_captured"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imagebutton_take_photo"
        android:gravity="center"
        android:text="@string/click_save_to_proceed"
        android:textSize="22sp"
        android:visibility="invisible" />

    <EditText
        android:id="@+id/edittext_caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button_save_image"
        android:gravity="center"
        android:hint="@string/enter_caption" />

    <Button
        android:id="@+id/button_save_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/save_image" />
</RelativeLayout>
